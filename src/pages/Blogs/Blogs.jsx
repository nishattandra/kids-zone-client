import React from 'react';
import useTitle from '../../hooks/useTitle';

const Blogs = () => {
    useTitle('blogs')
    return (
        <div>
            hello
        </div>
    );
};

export default Blogs;